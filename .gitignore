# === Python 基本忽略 ===
__pycache__/
*.py[cod]

# === 虚拟环境 ===
venv/
.env

# === 运行时产生的临时文件 ===
*.png
*.log
*.mks

# === PyInstaller 打包生成 (新增) ===
# build 和 dist 文件夹是生成的垃圾和结果，不上传
build/
dist/
# 注意：build.spec 是你手动写的打包配置，属于源码，需要上传！

# === 例外规则 (强制上传的资源) ===
# !表示“不忽略”，即强制跟踪这些文件
!mascot.png
!app.ico

# === 本地配置与个人文件 ===
# 用户存档，不上传 (data.json 是本地运行生成的)
data.json
# 个人笔记
想法.md

# === 其他文件夹 ===
old/
assets/